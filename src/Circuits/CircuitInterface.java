package Circuits;

public interface CircuitInterface {
    /*
     * what does our circuit do?
     * what do we want to do *with* our circuit?
     * -> test it, for one thing.
     * -> turn on/off (Vs = 0%; Vs = 100%) //can model this however we like
     *
     */
    // implementation of linked list, based on (http:///www.javatpoint.com/java-list)

    /**
     *
     * @return Returns true if component was added, false if the component could not be added
     */
    //public Boolean AddNode(Node n);


    /**
     *
     * @return Returns true if component was removed, false if the component could not be removed
     */
    //public Boolean RemoveNode(Node n);

    /**
     *
     * @param components a LinkedList containing 1 or more components.
     * @return the total voltage across the components in the list; (e.g. the voltage across one resistor or the voltage across two resistors)
     */
    //public Voltage GetVoltageAcross(LinkedList<Component> components);

}
