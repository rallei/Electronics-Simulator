package Circuits;

public class VoltageDivider extends Circuit{

    /*
     * Start with voltage gain (must also be calculable quantity)
     * Va = Vout/Vin
     *
     * may need to know how electrons/current can flow in a circuit (if not series)
     * need:
     * 1. current (in series, one choice)
     * 2. voltage (in series, two choices - Vin, Vout)
     */
}
