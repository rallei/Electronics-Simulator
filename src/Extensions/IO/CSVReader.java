package Extensions.IO;

public class CSVReader {
    /* Rules:
     * Comma = separator
     * First line = categories
     * Second line = values in each category
     *
     * More formally:
     * let O = each comma separated object
     *
     * // gather the *names* of all of the fields
     * List<string> fieldNames ...
     *
     * foreach(object O in firstLine){
     * fieldNames.Add(O.ToString());
     * }
     *
     *
     */
}
