package Extensions;

public class Constants {
    public static final int NUM_DIRECTIONS = 4; // 3, because... 0,1,2,3 are *four* choices! for top, right, bottom, left (or north, east, south, west)
}
